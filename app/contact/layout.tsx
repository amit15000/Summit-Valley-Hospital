import type { Metadata } from "next";
export const metadata:Metadata={title:"Contact Us",description:"Contact Summit Valley Medical Center in Austin for hospital, accessibility, insurance, appointment, or career information."};
export default function ContactLayout({children}:{children:React.ReactNode}){return children}
